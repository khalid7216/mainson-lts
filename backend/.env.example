# ── Server ───────────────────────────
PORT=
NODE_ENV=

# ── MongoDB ──────────────────────────
MONGO_URI=

# ── JWT ──────────────────────────────
JWT_SECRET=
JWT_EXPIRES_IN=

# ── Client URL (for CORS) ─────────────
CLIENT_URL=

# ── Email (Nodemailer) ─────────────────
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
FROM_NAME=
FROM_EMAIL=
STRIPE_SECRET_KEY=
